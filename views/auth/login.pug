extends ../root-layout

block body

	div.row.page-title: div.col-xs-12
		h1 Login to Your Account

	div.row: div.col-xs-12.col-md-6
		form(method = "post", action = "/auth/login")
			div.form-group
				label(for="username") Email Address
				input#username.form-control(type="email", name="username", value="me@markormesher.co.uk")

			div.form-group
				label(for="password") Password
				input#password.form-control(type="password", name="password", value="password")

			div.form-group
				button.btn.btn-primary(type = "submit") Login
